{
  "name": "cashregister",
  "version": "1.0.0",
  "description": "## The Problem Creative Cash Draw Solutions is a client who wants to provide something different for the cashiers who use their system. The function of the application is to tell the cashier how much change is owed, and what denominations should be used. In most cases the app should return the minimum amount of physical change, but the client would like to add a twist. If the \"owed\" amount is divisible by 3, the app should randomly generate the change denominations (but the math still needs to be right :))",
  "main": "index.js",
  "scripts": {
    "start:server": "npx ts-node -r tsconfig-paths/register server/src/index.ts",
    "start:client": "cd client && react-scripts start",
    "build:server": "rm -rf server/dist && tsc --project server/tsconfig.json",
    "build:client": "cd client && react-scripts build",
    "build": "npm run build:server && npm run build:client",
    "docker:up": "docker-compose -f docker-compose.yml up --build",
    "docker:down": "docker-compose -f docker-compose.yml down",
    "migrate:up": "ts-node -r tsconfig-paths/register node_modules/.bin/knex migrate:latest --knexfile server/knexfile.ts",
    "migrate:down": "ts-node -r tsconfig-paths/register node_modules/.bin/knex migrate:rollback --knexfile server/knexfile.ts",
    "migrate:create": "ts-node -r tsconfig-paths/register node_modules/.bin/knex migrate:make --knexfile server/knexfile.ts -x ts",
    "start": "concurrently \"npm run start:server\" \"npm run start:client\"",
    "prestart": "npm run migrate:up",
    "lint": "eslint . --ext .js,.ts,.jsx,.tsx",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "body-parser": "^1.20.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "fs": "^0.0.1-security",
    "knex": "^3.1.0",
    "multer": "^1.4.5-lts.1",
    "path": "^0.12.7",
    "pg": "^8.12.0",
    "typescript": "^4.9.5"
  },
  "devDependencies": {
    "@babel/register": "^7.24.6",
    "@types/express": "^4.17.21",
    "@types/fs-extra": "^11.0.4",
    "@types/jest": "^29.5.12",
    "@types/multer": "^1.4.11",
    "@types/node": "^20.14.12",
    "@typescript-eslint/eslint-plugin": "^7.17.0",
    "@typescript-eslint/parser": "^7.17.0",
    "concurrently": "^8.2.2",
    "eslint": "^8.57.0",
    "eslint-config-airbnb": "^19.0.4",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-import-resolver-typescript": "^3.6.1",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-prettier": "^5.2.1",
    "eslint-plugin-react": "^7.35.0",
    "eslint-plugin-react-hooks": "^4.6.2",
    "jest": "^29.7.0",
    "prettier": "^3.3.3",
    "sucrase": "^3.35.0",
    "ts-jest": "^29.2.3",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.10",
    "tsconfig-paths": "^4.2.0"
  }
}
